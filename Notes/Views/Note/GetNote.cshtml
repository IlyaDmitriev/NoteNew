@using Notes.ViewModels;
@model GetNote
@{
	var note = Model.Note;
}

@if (!Model.AlreadyDeleted && Model.Note != null)
{
	<h1>Привет, @Model.Note.UserName</h1>
	

	<div class="jumbotron jumbotron-fluid">
		<div class="container">
			<h1 class="display-4">Вот твоя заметка:</h1>
			<p class="lead">@Model.Note.NoteText</p>
		</div>
	</div>
}
<div>@Model.Message</div>
<br />

